# IoT Agent システム構成図 (卒業論文用)

このドキュメントでは、本研究におけるIoT Agentシステムの全体アーキテクチャをMermaid記法で記述します。
ユーザーの自然言語指示がどのように処理され、物理デバイスのアクションに変換されるかを可視化しています。

## システムアーキテクチャ図

```mermaid
graph TD
    %% スタイル定義
    classDef user fill:#ffe0b2,stroke:#e65100,stroke-width:2px;
    classDef server fill:#bbdefb,stroke:#0d47a1,stroke-width:2px;
    classDef cloud fill:#e1bee7,stroke:#4a148c,stroke-width:2px;
    classDef edge fill:#c8e6c9,stroke:#1b5e20,stroke-width:2px;
    classDef internal fill:#ffffff,stroke:#607d8b,stroke-width:1px,stroke-dasharray: 5 5;

    %% ノード定義: ユーザー層
    subgraph "User Interface Layer"
        User((ユーザー)):::user
        Browser[Webブラウザ<br>(Chat UI)]:::user
    end
    
    %% ノード定義: サーバー層
    subgraph "IoT Agent Server (Central System)"
        APIGateway[API Gateway / Controller<br>(Flask: app.py)]:::server
        
        subgraph "Internal Logic"
            StateManager[状態管理・DB<br>(state.py / In-Memory)]:::internal
            JobQueue[ジョブキュー]:::internal
            LLMHandler[LLMハンドラ / プロンプトエンジニアリング<br>(llm.py)]:::internal
            ModelSelector[モデル選択ロジック<br>(model_selection.py)]:::internal
        end
    end

    %% ノード定義: インテリジェンス層
    subgraph "Intelligence Layer (Cloud)"
        LLM_API[LLM API<br>(OpenAI GPT-4o / Google Gemini / Anthropic Claude)]:::cloud
    end

    %% ノード定義: 物理デバイス層
    subgraph "Edge Device Layer (Physical World)"
        Jetson[NVIDIA Jetson<br>(AI処理・画像認識)]:::edge
        RPi[Raspberry Pi 4<br>(汎用制御・カメラ)]:::edge
        Pico[Raspberry Pi Pico W<br>(リアルタイム制御・センサー)]:::edge
    end

    %% データフロー
    User -- "1. 自然言語で指示 (例: '部屋の温度を教えて')" --> Browser
    Browser -- "2. HTTP POST (JSON)" --> APIGateway
    
    APIGateway -- "3. コンテキスト構築" --> LLMHandler
    LLMHandler -- "モデル選択" --> ModelSelector
    LLMHandler -- "4. 推論リクエスト" --> LLM_API
    LLM_API -- "5. 解釈結果 (Function Call / JSON)" --> LLMHandler
    
    LLMHandler -- "6. ジョブ生成" --> StateManager
    StateManager -- "キューイング" --> JobQueue
    
    %% ポーリングメカニズム
    Jetson -- "7. ポーリング (GET /jobs/next)" --> APIGateway
    RPi -- "7. ポーリング (GET /jobs/next)" --> APIGateway
    Pico -- "7. ポーリング (GET /jobs/next)" --> APIGateway
    
    JobQueue -.-> APIGateway
    
    APIGateway -- "8. コマンド配信 (例: servo_angle=90)" --> Jetson
    APIGateway -- "8. コマンド配信" --> RPi
    APIGateway -- "8. コマンド配信" --> Pico
    
    Jetson -- "9. 物理アクション実行 & 結果送信" --> APIGateway
    RPi -- "9. 物理アクション実行 & 結果送信" --> APIGateway
    Pico -- "9. 物理アクション実行 & 結果送信" --> APIGateway
    
    APIGateway -- "10. 結果通知・UI更新" --> Browser
    Browser -- "完了表示" --> User
```

## 各コンポーネントの役割

1.  **User Interface Layer**:
    *   ユーザーとの接点となるWebアプリケーション。チャット形式で直感的な操作を提供します。

2.  **IoT Agent Server (Central System)**:
    *   システムの中核となるFlaskアプリケーション。
    *   **LLMハンドラ**: ユーザーの曖昧な指示を、デバイスが理解可能な構造化データ（JSON）に変換します。
    *   **状態管理**: 接続されているデバイスのヘルスチェックと、実行待ちジョブの管理を行います。

3.  **Intelligence Layer**:
    *   高度な自然言語処理能力を持つ外部AIモデル。複雑な指示の意図理解や、タスクの分解を担当します。

4.  **Edge Device Layer**:
    *   **Jetson**: 画像処理やエッジAI推論など、高い計算能力を要するタスクを実行。
    *   **Raspberry Pi**: カメラ撮影や一般的なGPIO制御を担当。
    *   **Pico W**: 低消費電力で常時稼働し、センサー監視やモーター制御などのリアルタイム処理を担当。
